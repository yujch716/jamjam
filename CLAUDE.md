# JamJam

iOS 멀티플레이 리듬게임. 사용자가 등록한 mp3를 온디바이스 AI가 분석해 악기별(기타/드럼/베이스/피아노) 채보를 자동 생성한다.

## 개요
- 플랫폼: iOS, **iPad 전용**, 세로 화면 고정
- 개발: 개인 개발, 정해진 기간 없음
- 핵심 컨셉: 등록한 mp3 → AI가 악기별 채보 자동 생성 → 멀티플레이 리듬게임
- 실행 환경: **완전 오프라인**. 서버 통신 없이 등록~플레이 전부 앱 단독 처리 (온디바이스 AI). 로그인/계정 없음

## 기술 스택
- Swift + SwiftUI
- SpriteKit: 노트 애니메이션
- AVAudioEngine: 오디오 재생/싱크
- UIDocumentPicker: 로컬 mp3 파일 등록
- Core ML: AI 채보 생성 파이프라인을 앱 내부에서 직접 실행
  - **소스 분리**: htdemucs_6s (Meta demucs 공식 6-소스 사전학습 모델, guitar/piano 스템 포함) → ONNX → Core ML(.mlpackage) 변환, Neural Engine에서 추론
  - **온셋 검출**: 자체 학습한 경량 CNN (멜스펙트로그램 입력) → Core ML 변환
- 모델 학습(Python/PyTorch)은 별도 오프라인 환경에서 1회성으로 진행, 결과 가중치만 앱에 내장

## 화면 플로우

홈 → 노래 리스트 → 인원수(1/2/4인) 및 악기 선택 → 게임 화면(세로 고정) → 결과(점수)

### 홈 화면
- 앱 진입점, 노래 리스트로 이동. 온보딩/튜토리얼 없음

### 노래 리스트 화면
- 등록된 음원 목록(제목), 신규 음원 등록 버튼(mp3만 지원)
- 등록 시 온디바이스 AI 채보 생성 (비동기, 진행률 표시). 실패 시 별도 재시도 없이 Alert만 표시
- 채보 생성 완료된 곡만 플레이 가능
- 곡 삭제 시 원본+분리트랙+채보 전부 함께 삭제
- 즐겨찾기 기능
- 자동 생성된 채보는 사용자가 수정 불가
- 중복 등록 방지 로직 없음 (같은 곡 여러 번 등록 가능, 관리는 사용자 책임)

### 인원/악기 선택 화면
- 곡 선택 시 미리듣기 재생
- 인원수 선택: 1인 / 2인 / 4인
- 인원수만큼 악기 선택 슬롯 노출 (기타/드럼/베이스/피아노 중 중복 없이 선택, 곡에 존재하는 악기 트랙만 선택 가능)

### 게임 화면 (세로 고정)
- 화면은 인원수만큼 분할된 개별 플레이 창으로 구성
  - 1인: 창 1개, 전체화면
  - 2인: 세로 위/아래 2분할. 아래쪽 창은 위쪽 창 대비 노트 배치가 거울 대칭 (아이패드를 사이에 두고 마주 앉아 플레이)
  - 4인: 창 4개 분할
- 창 1개당 최대 동시 터치 노트 2개로 제한
- 노트 종류: 탭(Tap), 홀드(Hold) 두 종류, 모든 악기 공통
- 진동(햅틱) 피드백 없음 (시각+청각으로만 판정 전달)

### 판정 타이밍 윈도우
| 판정 | 범위 |
|---|---|
| Perfect | ±20ms |
| Great | ±50ms |
| Good | ±100ms (점수 없지만 콤보 유지) |
| Bad | ±150ms |
| Miss | ±150ms 초과 또는 터치 안 함 |

홀드 노트:
- 시작 판정은 위 기준과 동일
- 도중에 손을 떼면 그 이후 구간 Miss
- 종료(릴리즈)도 ±100ms 이내에 떼야 정상 완료, 아니면 그 구간만 Bad

### 난이도
- 별도 난이도 구분 없이 채보 1개만 생성 (온셋 검출 임계값을 촘촘하게 잡아 기본적으로 어렵게)
- 실질 난이도는 인원수/악기 선택 개수로 조절됨 (혼자 4악기 다 담당하면 매우 어려움)

### 결과 화면
- 플레이어(악기 창)별: 판정분포(Perfect/Great/Good/Bad/Miss 개수), 최대 콤보, 획득 점수, 달성률(%), 등급
- 전체 합산 점수 + 종합 등급도 함께 표시
- 다시하기 / 홈으로 이동

## 디자인 테마 (탭소닉 Tapsonic 스타일)
- 어두운 배경(네이비/블랙) + 네온 컬러 강조
- 악기별 고유 네온 컬러: 기타=핑크/마젠타, 드럼=시안, 베이스=옐로우/오렌지, 피아노=퍼플
- 판정 시 네온 글로우/파티클 이펙트, 발광 폰트로 판정 텍스트 표시 후 페이드아웃
- 콤보 증가에 따라 배경 글로우/파티클 강도 점진적 강화
- UI 컴포넌트는 어두운 카드 배경 + 네온 테두리/글로우 라인
- 굵고 각진 산세리프 폰트
- 구현: SwiftUI shadow/blur로 글로우, SpriteKit SKEmitterNode로 파티클

## 점수 집계 시스템

### 판정별 기본 점수
| 판정 | 기본 점수 | 콤보 |
|---|---|---|
| Perfect | +2점 | 증가 |
| Great | +1점 | 증가 |
| Good | 0점 | 유지(증가) |
| Bad | 0점 | 끊김(0으로 리셋) |
| Miss | -1점 | 끊김(0으로 리셋) |

### 콤보 가중치
- 최종 획득 점수 = 기본 판정 점수 × 콤보 배율
- 콤보 배율 = `1 + floor(현재 콤보 / 10) × 0.1` (10콤보마다 10%씩 증가, 100콤보 이상부터 x2.0 고정)
- 단, Miss(-1점)에는 배율 적용하지 않고 고정 -1점

### 악기 창별 등급
- 달성률(%) = (실제 획득 점수 ÷ 만점) × 100
- 만점 = 전체 노트 수 × 2점 (전곡 Perfect 기준, 콤보 가산점 제외)

| 등급 | 달성률 |
|---|---|
| SSS | 100% |
| SS | 95~99.9% |
| S | 90~94.9% |
| A | 80~89.9% |
| B | 70~79.9% |
| C | 60~69.9% |
| D | 50~59.9% |
| F | 50% 미만 |

## 데이터 모델 (로컬 저장, 서버 없음)

로그인/계정 없음. 앱 삭제 시 모든 데이터(원본음원, 분리트랙, 채보, 플레이기록) 함께 삭제됨. iCloud 백업 미지원.

```
Song
- id, title, local_file_path, duration
- status (importing / processing / ready / failed)
- isFavorite (bool)

InstrumentTrack
- song_id, instrument (guitar/drum/bass/piano)
- separated_audio_path
- note_chart (json: [{time, type: tap|hold, duration?}])

PlaySession
- song_id, player_count, instruments_selected[]
- results: [{player, instrument, score, accuracy, grade}]
```

## AI 파이프라인 (온디바이스, 서버 없음)
1. 사용자가 mp3 등록 (인터넷 불필요)
2. 기기 내 Core ML 파이프라인: htdemucs_6s로 소스 분리 → 분리 트랙별 온셋 CNN으로 노트(탭/홀드) 생성
3. 결과(채보 JSON) + 원본음원 + 분리트랙을 로컬에 저장
4. 처리 시간(수십초~수분) 동안 비동기 처리 + 진행률 UI
5. 실패 시 Alert만 표시, 재시도 로직 없음

## 알려진 리스크
- htdemucs_6s의 피아노 소스 분리 품질이 아직 불완전함 (공식적으로 명시된 한계)
- 온디바이스 추론 처리 시간/발열/배터리 소모
- Core ML 모델(수백MB급) 앱 번들 용량 증가
- 저장 용량: 중복 등록 방지 없어 사용자가 직접 삭제로 관리해야 함
