# JamJam

iOS 멀티플레이 리듬게임. 사용자가 등록한 mp3를 온디바이스 AI가 분석해 악기별(기타/드럼/베이스/피아노) 채보를 자동 생성한다.

## 개요
- 플랫폼: iOS, **iPad 전용**, 세로 화면 고정
- 개발: 개인 개발, 정해진 기간 없음
- 핵심 컨셉: 등록한 mp3 → AI가 악기별 채보 자동 생성 → 멀티플레이 리듬게임
- 실행 환경: **완전 오프라인**. 서버 통신 없이 등록~플레이 전부 앱 단독 처리 (온디바이스 AI). 로그인/계정 없음
- 기본 제공곡 없음 — 앱에 번들된 데모/샘플 곡이 전혀 없고, 사용자가 직접 mp3를 등록해야만 플레이할 수 있다

## 기술 스택
- Swift + SwiftUI
- SpriteKit: 노트 애니메이션
- AVAudioEngine: 오디오 재생/싱크 (게임 화면에서 원곡을 재생, 분리 트랙은 채보 생성에만 사용)
- UIDocumentPicker: 로컬 mp3 파일 등록
- Core ML: AI 채보 생성 파이프라인을 앱 내부에서 직접 실행 — **실제로 온디바이스에서 구현·검증 완료된 상태** (실기기 기준 약 4분짜리 곡 하나에 소스 분리 ~19초 + 온셋 검출 ~1.5초)
  - **소스 분리**: htdemucs_6s (Meta demucs 공식 6-소스 사전학습 모델, guitar/piano 스템 포함) → Core ML(.mlpackage) 변환, Neural Engine에서 추론. STFT/ISTFT는 Core ML이 복소수 텐서를 지원하지 않아 Swift/Accelerate로 직접 구현
  - **온셋 검출**: 자체 학습한 경량 CNN (멜스펙트로그램 입력) → Core ML 변환. 학습 데이터는 기타/드럼/베이스/보컬만 사용 — **피아노는 전용 학습 없이 같은 모델을 그대로 적용(일반화)**, htdemucs_6s 자체의 피아노 분리 품질 한계와 맞물려 정확도가 상대적으로 낮을 수 있음
- 모델 학습(Python/PyTorch)은 별도 오프라인 환경에서 1회성으로 진행, 결과 가중치만 앱에 내장 — Core ML 모델(.mlpackage)은 파일 크기가 커서(demucs 모델 약 150MB) **Git LFS**로 관리

## 화면 플로우

홈 → 노래 리스트 → 인원수(1/2/4인) 및 악기 선택 → 게임 화면(세로 고정) → 결과(점수)

### 홈 화면
- 앱 진입점, 노래 리스트로 이동. 온보딩/튜토리얼 없음

### 노래 리스트 화면
- 등록된 음원 목록(제목), 신규 음원 등록 버튼(mp3만 지원)
- 등록 시 온디바이스 AI 채보 생성 (비동기, 진행률 표시). 실패 시 별도 재시도 없이 Alert만 표시 + 실패 사유를 곡 항목에 저장해 나중에도 확인 가능
- 처리 중 화면 잠금/백그라운드 전환으로 중단되지 않도록 처리 중 화면 자동 잠금 방지 + 백그라운드 유예 확보, 경고 문구 표시. 그래도 중단되면 다음 앱 실행 시 자동으로 실패 처리(무한 "처리 중" 방지)
- 채보 생성 완료된 곡만 플레이 가능
- 곡마다 삭제 버튼이 항상 보이는 형태로 노출(스와이프 삭제도 병행 지원) — 삭제 시 원본+분리트랙+채보 전부 함께 삭제, 실패 시 Alert
- 즐겨찾기 기능: 별 아이콘으로 토글, "전체"/"⭐ 즐겨찾기" 필터 탭으로 즐겨찾기한 곡만 모아보기 가능. "전체" 탭에서는 즐겨찾기 곡이 상단에 고정
- 곡을 인원수별(1인/2인/4인) 최고 기록이 하나라도 있으면 그중 가장 높은 달성률의 등급 배지를 곡 제목 옆에 표시
- 자동 생성된 채보는 사용자가 수정 불가
- 중복 등록 방지 로직 없음 (같은 곡 여러 번 등록 가능, 관리는 사용자 책임)

### 인원/악기 선택 화면
- 인원수 선택: 1인 / 2인 / 4인
- 인원수만큼 악기 선택 슬롯 노출 (기타/드럼/베이스/피아노 중 중복 없이 선택, 곡에 존재하는 악기 트랙만 선택 가능)
- 이 곡의 1인/2인/4인 각각의 최고 점수/등급을 표시 (아직 플레이 안 한 인원수는 "기록 없음")

### 게임 화면 (세로 고정)
- 화면은 인원수만큼 분할된 개별 플레이 창으로 구성. 각 창은 독립적인 RhythmScene+GameState 인스턴스 (노트/판정/홀드유닛채점/콤보/점수 로직은 1인 모드와 완전히 동일하게 재사용, 서로 완전히 분리된 상태로 동시 진행)
  - 1인: 창 1개, 전체화면
  - 2인: 세로 위/아래 2분할. **좌우 거울 대칭이 아니라 위쪽 창 전체를 180도 회전** — 판정영역이 각자 화면 바깥쪽 가장자리(아래쪽 창은 6시 방향, 위쪽 창은 12시 방향)에 위치 (아이패드를 사이에 두고 마주 앉아 플레이, 각자 시야에서는 노트가 자기 앞으로 떨어지는 정상 방향으로 보임)
  - 4인: 위/아래 2분할을 각각 세로선으로 한 번 더 쪼갠 2x2 구성. 아래 두 구역은 6시 방향(정상), 위 두 구역은 12시 방향(180도 회전) — 같은 절반 안의 좌/우 두 구역끼리는 서로 좌우 반전 없이 동일 방향
  - 레인 개수(4개)는 인원수와 무관하게 각 창마다 동일, 창 크기에 비례해 노트/판정영역/폰트 크기 축소
  - 모든 창이 정확히 하나의 공유 AVAudioEngine 인스턴스로 원곡을 재생 (창마다 따로 재생하면 서로 어긋난 사본이 겹쳐 들리기 때문)
  - 모든 창의 노트가 끝나면(가장 늦게 끝나는 창 기준) 전체 종료, 결과 화면에 창(플레이어)별 결과 + 전체 합산 점수/종합 등급 표시
- 창 1개당 최대 동시 터치 노트 2개로 제한 — 채보 생성 시 강한 온셋을 의도적으로 동시 2레인 노트로 승격시켜 이 규칙이 실제로 쓰이게 함 (아래 "채보 생성 세부 로직" 참고)
- 노트 종류: 탭(Tap), 홀드(Hold) 두 종류, 모든 악기 공통. 노트 모양은 알약형(스타디움 셰이프), 판정 영역은 판정선 위아래로 걸친 반투명 사각 zone으로 표시 (터치 히트박스 자체는 타이밍 윈도우 기반으로 별도 계산, 화면에 보이는 zone보다 실제로는 더 넓음)
- 진동(햅틱) 피드백 없음, 판정 효과음(SFX)도 없음 — **판정은 오직 시각 효과(네온 글로우/파티클/텍스트)로만 전달**
- 터치 시작/유지/종료에 따라 해당 레인이 즉시 밝아지는 프레스 피드백 (판정 성공/실패와 무관하게, "지금 이 레인을 누르고 있다"는 것 자체를 알려주는 용도)
- 화면 중앙에 현재 콤보 수를 큰 숫자로 항상 표시 (판정마다 즉시 갱신, 네온 글로우 + 팝업 애니메이션)
- 콤보가 오를수록 배경 글로우가 10콤보 단위로 단계적으로 강해지고(100콤보 이상에서 최대 강도 + 은은한 펄스), 판정 파티클 밀도도 동반 상승. Bad/Miss로 콤보가 끊기면 즉시 약해짐 — 노트 색상(아래 참고)과 같은 컬러로 표시되어 창마다 자기 색으로 반응
- 각 창 상단 중앙에 담당 악기 이름+이모지 배지 표시 (예: 🎸 기타) — 멀티플레이에서 창끼리 구분하기 위함, 해당 악기의 네온 컬러 사용
- **노트/레인 색상은 악기가 아니라 플레이어(창) 기준**: 1인 플레이는 담당 악기와 무관하게 항상 하늘색 하나로 통일, 2인/4인은 창마다 서로 다른 네온 컬러(플레이어1=하늘색 고정, 이후 플레이어는 핑크/오렌지/퍼플) — 악기 이름+이모지 배지만 악기별 색을 계속 사용

### 판정 타이밍 윈도우
| 판정 | 범위 |
|---|---|
| Perfect | ±20ms |
| Great | ±50ms |
| Good | ±100ms (점수 없지만 콤보 유지) |
| Bad | ±150ms |
| Miss | ±150ms 초과 또는 터치 안 함 |

홀드 노트 (유닛 단위 채점 — 아래 "점수 집계 시스템" 참고):
- 홀드 노트는 0.15초 간격의 "유닛"으로 쪼개져서 유닛마다 각각 판정/점수/콤보가 매겨짐 (탭 노트는 항상 유닛 1개)
- 첫 유닛: 터치 시작 오프셋으로 위 5단계 판정 그대로 적용
- 중간 유닛들: 그 구간 내내 눌려 있었으면 Perfect, 중간에 손을 떼고 있었으면 Miss
- 마지막 유닛(릴리즈): 종료 시점 100ms 전부터 계속 누르고 있었다면 그 이후 언제 손을 떼든(늦게 떼거나 아예 안 떼도) Perfect — 릴리즈 타이밍 자체를 정밀하게 맞출 필요 없음
- 종료 윈도우 진입 전에 조기 릴리즈하면 그 시점 이후 남은 유닛 전부 Miss로 한꺼번에 처리 (긴 홀드를 일찍 놓을수록 더 큰 손해)

### 난이도
- 별도 난이도 구분 없이 채보 1개만 생성 (온셋 검출 임계값을 촘촘하게 잡아 기본적으로 어렵게)
- 실질 난이도는 인원수/악기 선택 개수로 조절됨 (혼자 4악기 다 담당하면 매우 어려움)

### 채보 생성 세부 로직 (온디바이스 AI 파이프라인)
1. htdemucs_6s로 소스 분리 (긴 곡은 50% 오버랩 청크로 나눠 처리)
2. 악기별 분리 트랙마다 온셋 CNN으로 프레임별 온셋 확률 산출
3. 트랙 자체 최대 RMS 대비 상대적으로 너무 조용한 구간은 온셋 후보에서 미리 제외 (에너지 게이트 — 분리 잡음으로 인한 오탐 방지, 4개 악기 전체에 공통 적용)
4. 피크피킹(지역 최댓값 + 최소 간격)으로 온셋 이벤트 확정
5. 온셋 직후 RMS 엔벨로프가 유지되는 시간으로 탭/홀드 판정
6. 레인 배정: 온셋마다 4개 레인 중 **순수 무작위**로 배정(직전 레인과 달라야 한다는 제약 없음 — 같은 레인 연속 등장도 정상적인 결과)
7. 온셋 강도(로컬 RMS 피크) 상위 약 17.5%는 동시 2레인 노트로 승격 — 승격 시 주변 ±150ms 내 이미 쓰인 레인은 피해서 2개를 고르고, 2개를 고를 수 없으면 승격을 취소하고 단일 레인으로 폴백

### 결과 화면
- 플레이어(악기 창)별: 담당 악기 이름+이모지, 판정분포(Perfect/Great/Good/Bad/Miss 개수 — 홀드 노트는 유닛 단위로 여러 번 집계됨), 최대 콤보, 획득 점수, 달성률(%), 등급
- 전체 합산 점수 + 종합 등급도 함께 표시
- 이 결과(전체 합산 점수/등급)가 그 곡의 해당 인원수 모드 기존 최고 기록보다 높으면 자동으로 갱신 (낮으면 그대로 유지 — 아래 "기록 저장" 참고)
- 다시하기 / 홈으로 이동

## 디자인 테마 (탭소닉 Tapsonic 스타일)
- 어두운 배경(네이비/블랙) + 네온 컬러 강조 — 게임 화면뿐 아니라 홈/노래 리스트/인원·악기 선택 화면도 동일한 톤과 스타일로 통일
- 게임 화면 노트/레인 색상은 플레이어별(위 "게임 화면" 참고), 악기 이름+이모지 배지만 악기별 고유 네온 컬러 사용: 기타=핑크/마젠타, 드럼=시안, 베이스=옐로우/오렌지, 피아노=퍼플
- 홈/노래 리스트/인원·악기 선택 화면은 멀티플레이 창이 아니므로 기본 네온 컬러(하늘색 — 1인 플레이와 동일한 색)를 공통 강조색으로 사용
- 판정 시 네온 글로우/파티클 이펙트, 발광 폰트로 판정 텍스트 표시 후 페이드아웃 (오디오 SFX 없음, 시각 효과로만 전달)
- 콤보 증가에 따라 배경 글로우/파티클 강도가 10콤보 단위로 점진적으로 강해짐 (100콤보 이상에서 최대 강도, Bad/Miss로 콤보가 끊기면 즉시 약해짐)
- UI 컴포넌트(버튼/리스트 카드/패널)는 어두운 카드 배경 + 네온 테두리/글로우 라인, 터치 시 스케일+밝기 하이라이트 애니메이션
- 굵고 각진 산세리프 폰트 (rounded 디자인)
- 구현: SwiftUI shadow/blur/RadialGradient로 글로우, SpriteKit SKEmitterNode로 판정 파티클

## 점수 집계 시스템

### 판정별 기본 점수
| 판정 | 기본 점수 | 콤보 |
|---|---|---|
| Perfect | +2점 | 증가 |
| Great | +1점 | 증가 |
| Good | 0점 | 유지(증가) |
| Bad | 0점 | 끊김(0으로 리셋) |
| Miss | -1점 | 끊김(0으로 리셋) |

### 홀드 노트 = 여러 "유닛"으로 분할 채점
- 탭 노트는 유닛 1개, 홀드 노트는 0.15초 간격으로 쪼갠 유닛 여러 개 (유닛 개수 = ceil(홀드 길이 ÷ 0.15초), 최소 1개)
- 유닛마다 독립적으로 판정·점수·콤보가 매겨짐 → 홀드가 길수록 점수/콤보에 기여하는 양(과 실패했을 때 손해)도 비례해서 커짐
- 판정 세부 규칙은 위 "판정 타이밍 윈도우 > 홀드 노트" 참고

### 콤보 가중치
- 최종 획득 점수 = 기본 판정 점수 × 콤보 배율
- 콤보 배율 = `1 + floor(현재 콤보 / 10) × 0.1` (10콤보마다 10%씩 증가, 100콤보 이상부터 x2.0 고정), **유닛 단위로 매번 갱신**
- 단, Miss(-1점)에는 배율 적용하지 않고 고정 -1점

### 악기 창별 등급
- 달성률(%) = (실제 획득 점수 ÷ 만점) × 100
- 만점 = 전체 유닛 수 × 2점 (전곡 Perfect 기준, 콤보 가산점 제외). 전체 유닛 수 = Σ(탭 노트 개수 × 1 + 각 홀드 노트의 유닛 개수)

| 등급 | 달성률 |
|---|---|
| SSS | 100% |
| SS | 95~99.9% |
| S | 90~94.9% |
| A | 80~89.9% |
| B | 70~79.9% |
| C | 60~69.9% |
| D | 50~59.9% |
| F | 50% 미만 |

### 기록 저장 (인원수별 최고 점수)
- 곡마다, **인원수(1인/2인/4인)별로 완전히 독립적인 최고 기록 1개만 저장** (곡 하나당 최대 3개)
- 저장 기준은 전체 합산 점수/등급(위 "결과 화면" 참고) — 새 기록의 합산 점수가 기존 최고 기록보다 높을 때만 갱신, 낮거나 같으면 기존 기록 유지
- 전체 플레이 이력(매 판마다의 로그)은 저장하지 않음 — 인원수별 최고 기록만 남김

## 데이터 모델 (로컬 저장, 서버 없음)

로그인/계정 없음. 앱 삭제 시 모든 데이터(원본음원, 분리트랙, 채보, 최고 기록) 함께 삭제됨. iCloud 백업 미지원.

```
Song
- id, title, original_file_name, duration
- status (importing / processing / ready / failed)
- error_message (실패 시 사유, 진단용으로 영속 저장)
- isFavorite (bool)
- available_instruments (실제 온셋이 검출된 악기만)
- best_results: { player_count(1/2/4) -> { score, max_possible_score, achievement_percent, grade } }

InstrumentTrack
- song_id, instrument (guitar/drum/bass/piano)
- separated_audio_path
- note_chart (json: [{time, type: tap|hold, duration?, lane}])
  # lane은 채보 생성 시점에 한 번 배정되어 저장됨 (로드할 때마다 다시 계산하지 않음)
```

## AI 파이프라인 (온디바이스, 서버 없음)
1. 사용자가 mp3 등록 (인터넷 불필요)
2. 기기 내 Core ML 파이프라인: htdemucs_6s로 소스 분리 → 분리 트랙별 온셋 CNN으로 노트(탭/홀드) 생성 → 레인 배정(무작위 + 동시 2노트 승격)
3. 결과(채보 JSON) + 원본음원 + 분리트랙을 로컬에 저장
4. 처리 시간(수십초~수분) 동안 비동기 처리 + 진행률 UI, 처리 중 화면 잠금/백그라운드 전환 방지
5. 실패 시 Alert만 표시 + 실패 사유 저장, 재시도 로직 없음

## 알려진 리스크
- htdemucs_6s의 피아노 소스 분리 품질이 아직 불완전함 (공식적으로 명시된 한계)
- 온셋 검출 CNN은 기타/드럼/베이스/보컬로만 학습됨 — 피아노는 전용 학습 데이터 없이 같은 모델을 그대로 적용(일반화)하므로 다른 악기 대비 정확도가 낮을 수 있음
- 온디바이스 추론 처리 시간/발열/배터리 소모
- Core ML 모델(수백MB급) 앱 번들 용량 증가 — Git LFS로 관리해 저장소 자체의 일반 git 오브젝트 용량 문제는 피했지만, 앱 번들 자체의 용량 증가는 여전함
- 저장 용량: 중복 등록 방지 없어 사용자가 직접 삭제로 관리해야 함
